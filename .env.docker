# Docker 環境變量配置
NODE_ENV=production
PORT=3000

# PostgreSQL 配置
DB_HOST=postgres
DB_PORT=5432
DB_NAME=webrtc_voice
DB_USER=webrtc_voice
DB_PASSWORD=your_secure_password_here

# 數據庫連接池配置
DB_POOL_MAX=10
DB_POOL_MIN=2

# CORS 配置
CORS_ORIGIN=*
